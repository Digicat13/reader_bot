namespace ReaderBot
{
    public enum NavigationType
    {
        Next = 1,
        Previous = 2,
    }

    public enum CallbackType
    {
        Download = 1,
        NextPage = 2,
        PreviousPage = 3,
        AddToList = 4,
    }
}
